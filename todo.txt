Add back in needing more context, as necessary, when it makes sense
Get the agent to look over everything for consistency - can it catch that the two figures should have the same shot numbers?
- something like, based on the structure/attributes of this evidence, is there any evidence that suspect?
- Or does it seem like the authors are selectively displaying certain data?
- Would you have expected the evidence to be presented in a different way?
Check that evidence is actually using the figure names.
Graph database for persistence?
Fix advisor type definitions
consolidate get_evaluation_branches


get equations connected
do simple citation checker
properly hook up the graph database
think more about the data graph - do we want the text from the document then evidence?  what is that structure?  Should we get rid of the text option?

Identity use cases to implement:
- LogoutUser
- RevokeToken
- GetCurrentUser

Refresh-token reuse detection (build on top of RefreshAccessToken rotation):
- Add family_id + status (active/rotated/revoked) to refresh token storage; new schema/table needed since PASETO adapter has no persistence layer yet
- Split verify_refresh_token port: raise TokenReuseDetected(family_id) when a rotated token is replayed; None stays for unknown/revoked
- Add revoke_family(family_id) port method; RefreshAccessToken catches reuse and revokes the whole family before re-raising
- HTTP: map to 401 with distinct code (e.g. "session_revoked") so frontend clears all auth state and redirects to login
- Emit a SuspectedTokenReuse domain event for side-channel reactions (logging, user email, on-call alerts) — keep use case free of alerting logic
- Mitigate false positives from tab races / retries: short rotation grace window (~5s accept-prior-token) or client-side request coalescing